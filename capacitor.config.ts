import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'viajes-admin-front-ionic',
  webDir: 'www'
};

export default config;
